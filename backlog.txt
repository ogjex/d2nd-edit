THE BIG FAT BACKLOG 

- Code Improvement
    - make code Object Oriented
        *- backup_manager
        - txt editor
        - json editor
    - unit test
        - backup_manager
            - test new restore_defaults function
        - txt editor
        - json editor
    - make code into MVP
    
*- create backup function
    *- create backup folder if not exists
    *- add char backup
    *- add appended numbers if multiple backups from same day
    - add char restore from backup
    - add optional comment on backups (saved as a .txt file)

*- create restore defaults
    *- restore all defaults

*- create settings file to read user preferences
    *- check for settings file and create if none exists
    *- add the column numbers so the edit column can be told which to edit

*- create logging function
    - add logging level
    
*- read file and edit specific columns and save
    *- read and edit monster density columns
    *- refactored function to freely define which columns to edit
    *- test with actual file (not test file)

*- create simple gui window

- GUI advanced
    - link buttons to backend
    *- add buttons and sliders
    *- move maximum slider along with minimum for umon
    *- add save button to lower left area
    *- add restore button default values to slider area
    - add logging window (bottom)
    - add rename of personal stash columns
    - add dropdown to determine logging level

